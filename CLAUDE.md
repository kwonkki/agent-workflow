# 프로젝트 규약 (CI 에이전트용)

이 파일은 CI에서 동작하는 Claude 에이전트가 리뷰/수정 시 따라야 할 규칙입니다.

## 코드 스타일
- Python 3.10+, 타입 힌트 필수.
- 포매팅/린트는 `ruff` 기준. PR 전에 `ruff check .` 통과해야 함.
- 함수는 순수 함수 우선, 부작용은 명시적으로.

## 테스트
- 모든 동작 변경에는 `tests/` 에 대응 테스트를 추가/수정한다.
- 테스트는 `pytest -q` 로 실행하며, 외부 네트워크에 의존하지 않는다.

## 리뷰 시 중점 사항 (Review 에이전트)
- 경계 조건: 0으로 나누기, 빈 입력, None, 오버플로.
- 입력 검증 누락 및 잘못된 타입.
- 명백한 정확성 버그 우선. 스타일 지적은 최소화.
- 발견이 없으면 "변경 사항 양호"라고 짧게 답한다. 억지 지적 금지.

## 수정 시 규칙 (Implement 에이전트)
- 요청 범위 밖의 리팩터링은 하지 않는다.
- 동작을 바꾸면 반드시 테스트를 함께 갱신한다.
- 커밋 메시지는 한 줄 요약 + 변경 이유.

## 금지
- `requirements.txt` 의 의존성을 임의로 추가/제거하지 않는다.
- 시크릿/토큰을 코드나 로그에 남기지 않는다.
